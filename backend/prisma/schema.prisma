
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model car {
  id        Int      @id @default(autoincrement())
  cost Int 
  name String
  system String
  mpg Int
  transmission String
  img String
}